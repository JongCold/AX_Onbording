# 시스템 아키텍처 및 워크플로우 안내서

본 문서는 **단기 근로자 온보딩 자동화 및 실시간 RAG(Retrieval-Augmented Generation) 챗봇 시스템**의 전체 구조와 작동 흐름을 한눈에 이해할 수 있도록 설계된 공식 아키텍처 가이드라인입니다.

---

## 1. 시스템 아키텍처 (Architecture)

시스템은 외부에 호스팅되는 **프론트엔드(Vercel)**와 대표자(관리자)의 로컬 환경에서 구동되는 **백엔드 서버(FastAPI + Ngrok)** 및 **인공지능 모델 인프라(Ollama)**가 유기적으로 연동되는 하이브리드 아키텍처를 채택하고 있습니다.

### 1.1 개념 아키텍처 다이어그램

```mermaid
graph TD
    %% 프론트엔드 영역
    subgraph Frontend [Vercel Cloud]
        UI[웹 대시보드 UI<br>HTML / CSS / JS]
    end

    %% 네트워크 터널링
    subgraph Network [Tunneling]
        Ngrok[Ngrok Free Tunnel]
    end

    %% 로컬 백엔드 및 DB 영역
    subgraph Backend_PC [Local PC Server]
        API[FastAPI API Engine]
        JSON_DB[(pending_onboardings.json)]
        Vector_DB[(vector_db.json)]
    end

    %% 외부 서드파티 인프라
    subgraph External_Services [External Platforms]
        Google_Drive[Google Drive Cloud Storage]
        Slack[Slack Workspace API]
    end

    %% 로컬 AI 모델 인프라
    subgraph AI_Engine [Local AI System]
        Ollama_Embed[Ollama Embed Engine<br>nomic-embed-text]
        Ollama_LLM[Ollama LLM Engine<br>Gemma2]
        Gemini[Gemini API Fallback]
    end

    %% 데이터 흐름
    UI -->|1. AJAX API 호출| Ngrok
    Ngrok -->|2. 포워딩| API
    API <-->|대기자 정보 저장/복원| JSON_DB
    API -->|3. 원본 문서 보관| Google_Drive
    API -->|4. 파일 분석 및 임베딩 요청| Ollama_Embed
    Ollama_Embed -->|5. 벡터 인덱싱 데이터 저장| Vector_DB
    API -->|6. 채널 개설 / 사용자 초대| Slack
    
    %% RAG 및 웹훅 흐름
    Slack -->|7. 가입 & 질문 이벤트| API
    API -->|8. 유사도 탐색| Vector_DB
    API -->|9. 답변 추론 요청| Ollama_LLM
    Ollama_LLM -->|Ollama 오프라인 시 Fallback| Gemini
    API -->|10. 챗봇 답변 & 파일 업로드| Slack
```

---

## 2. 주요 구성 요소 설명 (Components)

1. **프론트엔드 대시보드 (Vercel 호스팅)**
   - Glassmorphism(유리 질감) 테마가 적용된 관리자 전용 웹 대시보드입니다.
   - 근로자 기본 정보를 등록하고 드래그 앤 드롭으로 과업지시서와 온보딩 가이드 PDF를 간편하게 업로드할 수 있습니다.
   - 로컬 벡터 DB에 인덱싱된 문서 목록을 실시간으로 확인하고 선택해서 삭제할 수 있는 RAG 관리 창을 제공합니다.

2. **백엔드 API 서버 (FastAPI + Ngrok 터널링)**
   - 로컬 개발 환경에서 가동되며, Vercel과의 통신을 위해 Ngrok을 통한 보안 터널을 확보하고 CORS 보안을 우회 처리합니다.
   - 멀티파트 폼 데이터 수신, 구글 드라이브 업로드, RAG 임베딩, 슬랙 API 제어, 가입 대기자 영구 저장 및 슬랙 웹훅 이벤트를 핸들링합니다.

3. **로컬 RAG 검색 엔진 및 벡터 DB**
   - **임베딩**: `Ollama`의 `nomic-embed-text` 모델을 사용하여 업로드된 PDF의 문맥을 500자 단위 청크로 분할하여 로컬 `vector_db.json` 파일에 벡터화하여 인덱싱합니다.
   - **답변 추론**: 슬랙에서 질문이 들어왔을 때 관련 청크를 벡터 유사도(코사인 유사도)로 실시간 검색한 뒤 `Ollama` `Gemma2` 또는 `Gemini API`를 결합하여 상황에 최적화된 맞춤형 업무 지침 답변을 생성합니다.

4. **슬랙 연동 모듈 (Slack SDK)**
   - API를 사용하여 신입 근로자의 이메일 주소로 전용 OT 소통 채널(`#ot-proj-[이메일아이디]`)을 자동 개설하고 채널에 참여시킵니다.
   - PDF 자료 업로드 및 AI 생성 환영 메시지 배포를 관장합니다.

---

## 3. 핵심 워크플로우 (Workflow)

근로자의 슬랙 가입 상태에 따라 유동적으로 작동하는 **합류 시점 연동 지연 프로세스**가 포함되어 있습니다.

```mermaid
sequenceDiagram
    autonumber
    actor Admin as 관리자(대표)
    actor Worker as 신입 근로자
    participant Web as 웹 대시보드 (Vercel)
    participant Server as 백엔드 서버 (FastAPI)
    participant Slack as 슬랙 워크스페이스
    participant SMTP as 이메일 서버 (SMTP)

    Admin ->> Web: 근로자 인적사항 입력 및 PDF 업로드
    Web ->> Server: 온보딩 기동 API 요청 (POST /onboard-web)
    
    Note over Server: 1. 구글 드라이브 파일 업로드<br>2. RAG 벡터 DB 인덱싱 (Real-time Embedding)
    
    Server ->> Slack: 해당 이메일 기반으로 채널 초대 시도
    
    alt 신입 근로자가 슬랙 워크스페이스에 이미 가입된 경우
        Slack -->> Server: 초대 성공 (invited=True)
        Server ->> Slack: [즉시 실행] 과업/온보딩 PDF 파일 채널 업로드
        Server ->> Slack: [즉시 실행] 챗봇 환영 메시지 발송
        Server -->> Web: 온보딩 및 채널 연동 즉시 완료 응답
    else 신입 근로자가 슬랙 워크스페이스 미가입 상태인 경우
        Slack -->> Server: 초대 실패 (invited=False)
        Server ->> Server: [가입 대기자 정보 파일 보존]<br>pending_onboardings.json에 파일 경로 저장
        Server ->> SMTP: 근로자 이메일로 가입 링크 초대장 발송
        Server -->> Web: 가입 대기 상태로 이관 응답
        
        Note over Worker: 이메일 수신 후 가입 링크 클릭
        Worker ->> Slack: 슬랙 워크스페이스 가입 완료 (team_join 이벤트 발생)
        Slack ->> Server: team_join 웹훅 수신
        
        Note over Server: 대기자 파일(pending_onboardings.json)에서<br>근로자 매칭 및 지정되었던 고유 버전 파일 경로 추출
        
        Server ->> Slack: 근로자를 전용 프로젝트 채널에 직접 초대
        Server ->> Slack: 해당 채널에 보존했던 버전별 PDF 파일 업로드 (.verxx.pdf)
        Server ->> Slack: AI 분석 기반 정중한 환영 메시지 발송
    end

    Note over Worker, Slack: 질문 발생: @Auto_Bot1 질문 내용 입력
    Slack ->> Server: app_mention / message 이벤트 웹훅 전송
    Server ->> Server: RAG 유사도 검색 (vector_db.json) 및 답변 추론 (Ollama / Gemini)
    Server ->> Slack: 스레드 답글 형태로 답변 피드백 전송
```

### 3.1 RAG 지식 베이스 선택적 청소 워크플로우
- 과업 지시서나 온보딩 문서가 변경되어 불필요하거나 중복된 구버전 지식 삭제가 필요할 때 다음과 같은 흐름으로 지식을 정리합니다.
1. **조회**: 대시보드 하단의 'RAG 지식 베이스 관리' 영역에서 `GET /rag/sources` API를 호출하여 현재 RAG 데이터베이스에 축적되어 있는 원본 파일명 리스트를 가져와 출력합니다.
2. **삭제 요청**: 관리자가 특정 PDF 소스를 선택하여 '삭제' 버튼을 클릭하면 `DELETE /rag/sources?source=[파일명]` API가 백엔드로 전송됩니다.
3. **영구 삭제**: 백엔드 서버는 `vector_db.json`에서 해당 파일명(`metadata.source`)을 가진 모든 청크 벡터 데이터를 일괄 제거(필터링)하고 파일 시스템에 갱신 및 저장합니다.
4. **리로드**: 삭제 성공 메시지 수신 후 대시보드 목록이 동적으로 갱신되며, 슬랙 채널에서 이전 지식에 대한 질문 시 챗봇은 삭제된 지식에 관해 답변하지 않습니다.
